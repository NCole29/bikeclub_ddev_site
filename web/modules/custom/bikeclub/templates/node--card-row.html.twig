{# Renders content using the "card-row" component. #}

{{ include('bikeclub:card_row', {
  heading: node.title.value,
  url: path('entity.node.canonical', { node: node.id() }),
  image: content.banner_image,
  date: content.field_date,
  location: content.field_location,
  content: content.field_summary
}, with_context = false) }}
