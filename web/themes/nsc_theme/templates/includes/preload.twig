{#
/**
 * @file
 * Preload the commonly used fonts for the headings and the body copy.
 *
 * Available variables:
 * - nsc_theme_path: Returns the path to the NscTheme theme.
 */
#}

<link rel="preload" href="{{ nsc_theme_path }}/fonts/Outfit/latin.woff2" as="font" type="font/woff2" crossorigin />
